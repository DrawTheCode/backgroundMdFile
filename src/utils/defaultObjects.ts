import { bgmdStyle } from "src/types/bgmd";

export const defaultStyle = {
  pos: ['top','center'],
  rep: 'no-repeat',
  siz: 'cover',
  bgi: null,
  bgc: '0,0,0',
  opa: '.9',
  grd: ['right','gradient']
} as bgmdStyle;